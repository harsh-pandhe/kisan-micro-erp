# Kisan Micro-ERP is fully client-side (no backend, no cloud API).
# This file exists for Phase-1 tooling conventions; no runtime secrets are
# required today. Add build-time flags here only if a future milestone
# genuinely needs one (see docs/architecture.md).
